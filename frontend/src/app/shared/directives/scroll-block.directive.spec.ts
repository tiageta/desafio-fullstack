import { ScrollBlockDirective } from './scroll-block.directive';

describe('ScrollBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollBlockDirective();
    expect(directive).toBeTruthy();
  });
});
